services:
  - type: web
    name: alvaro-ai-backend
    env: node
    plan: free
    buildCommand: npm install && npx prisma db push && npx prisma generate && npx tsx server/src/seed.ts
    startCommand: npx tsx server/src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: alvaro-ai-super-secret-jwt-key-2026-fecap
      - key: GEMINI_API_KEY
        sync: false
